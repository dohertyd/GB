/* IS_AUTOGENERATED_SO_ALLOW_AUTODELETE=YES */
/* The previous line is to allow auto deletion */

package io.golgi.example.gen;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Iterator;
import com.openmindnetworks.golgi.JavaType;
import com.openmindnetworks.golgi.GolgiPayload;
import com.openmindnetworks.golgi.B64;
import com.openmindnetworks.golgi.api.GolgiException;

public class TapTelegraph_gameOver_reqArg
{

    private boolean corrupt = false;

    public boolean isCorrupt() {
        return corrupt;
    }

    public void setCorrupt() {
        corrupt = true;
    }

    private boolean gameOverDataIsSet = false;
    private GameOverData gameOverData;

    public GameOverData getGameOverData(){
        return gameOverData;
    }
    public boolean gameOverDataIsSet(){
        return gameOverDataIsSet;
    }
    public void setGameOverData(GameOverData gameOverData){
        this.gameOverData = gameOverData;
        this.gameOverDataIsSet = true;
    }

    public StringBuffer serialise(){
        return serialise(null);
    }

    public StringBuffer serialise(StringBuffer sb){
        return serialise("", sb);
    }

    public StringBuffer serialise(String prefix, StringBuffer sb){
        if(sb == null){
            sb = new StringBuffer();
        }

        if(this.gameOverDataIsSet){
            gameOverData.serialise(prefix + "" + 1 + "." , sb);
        }

        return sb;
    }

    private void deserialise(GolgiPayload payload){
        if(!isCorrupt() && payload.containsNestedKey("1")){
            GameOverData inst = new GameOverData(payload.getNested("1"));
            setGameOverData(inst);
               if(inst == null || inst.isCorrupt()){
                   setCorrupt();
               }
        }
        else {
            setCorrupt();
        }
    }

    public TapTelegraph_gameOver_reqArg(){
        this(true);
    }

    public TapTelegraph_gameOver_reqArg(boolean isSetDefault){
        super();
        gameOverDataIsSet = isSetDefault;
        gameOverData = new GameOverData(isSetDefault);
    }

    public TapTelegraph_gameOver_reqArg(GolgiPayload payload){
        this(false);
        deserialise(payload);
    }

    public TapTelegraph_gameOver_reqArg(String payload){
        this(JavaType.createPayload(payload));
    }

}
