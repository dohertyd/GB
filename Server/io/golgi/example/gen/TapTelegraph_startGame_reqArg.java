/* IS_AUTOGENERATED_SO_ALLOW_AUTODELETE=YES */
/* The previous line is to allow auto deletion */

package io.golgi.example.gen;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Iterator;
import com.openmindnetworks.golgi.JavaType;
import com.openmindnetworks.golgi.GolgiPayload;
import com.openmindnetworks.golgi.B64;
import com.openmindnetworks.golgi.api.GolgiException;

public class TapTelegraph_startGame_reqArg
{

    private boolean corrupt = false;

    public boolean isCorrupt() {
        return corrupt;
    }

    public void setCorrupt() {
        corrupt = true;
    }

    private boolean playerInfoIsSet = false;
    private PlayerInfo playerInfo;

    public PlayerInfo getPlayerInfo(){
        return playerInfo;
    }
    public boolean playerInfoIsSet(){
        return playerInfoIsSet;
    }
    public void setPlayerInfo(PlayerInfo playerInfo){
        this.playerInfo = playerInfo;
        this.playerInfoIsSet = true;
    }

    public StringBuffer serialise(){
        return serialise(null);
    }

    public StringBuffer serialise(StringBuffer sb){
        return serialise("", sb);
    }

    public StringBuffer serialise(String prefix, StringBuffer sb){
        if(sb == null){
            sb = new StringBuffer();
        }

        if(this.playerInfoIsSet){
            playerInfo.serialise(prefix + "" + 1 + "." , sb);
        }

        return sb;
    }

    private void deserialise(GolgiPayload payload){
        if(!isCorrupt() && payload.containsNestedKey("1")){
            PlayerInfo inst = new PlayerInfo(payload.getNested("1"));
            setPlayerInfo(inst);
               if(inst == null || inst.isCorrupt()){
                   setCorrupt();
               }
        }
        else {
            setCorrupt();
        }
    }

    public TapTelegraph_startGame_reqArg(){
        this(true);
    }

    public TapTelegraph_startGame_reqArg(boolean isSetDefault){
        super();
        playerInfoIsSet = isSetDefault;
        playerInfo = new PlayerInfo(isSetDefault);
    }

    public TapTelegraph_startGame_reqArg(GolgiPayload payload){
        this(false);
        deserialise(payload);
    }

    public TapTelegraph_startGame_reqArg(String payload){
        this(JavaType.createPayload(payload));
    }

}
