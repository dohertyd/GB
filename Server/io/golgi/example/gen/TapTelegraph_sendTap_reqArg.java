/* IS_AUTOGENERATED_SO_ALLOW_AUTODELETE=YES */
/* The previous line is to allow auto deletion */

package io.golgi.example.gen;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Iterator;
import com.openmindnetworks.golgi.JavaType;
import com.openmindnetworks.golgi.GolgiPayload;
import com.openmindnetworks.golgi.B64;
import com.openmindnetworks.golgi.api.GolgiException;

public class TapTelegraph_sendTap_reqArg
{

    private boolean corrupt = false;

    public boolean isCorrupt() {
        return corrupt;
    }

    public void setCorrupt() {
        corrupt = true;
    }

    private boolean tapDataIsSet = false;
    private TapData tapData;

    public TapData getTapData(){
        return tapData;
    }
    public boolean tapDataIsSet(){
        return tapDataIsSet;
    }
    public void setTapData(TapData tapData){
        this.tapData = tapData;
        this.tapDataIsSet = true;
    }

    public StringBuffer serialise(){
        return serialise(null);
    }

    public StringBuffer serialise(StringBuffer sb){
        return serialise("", sb);
    }

    public StringBuffer serialise(String prefix, StringBuffer sb){
        if(sb == null){
            sb = new StringBuffer();
        }

        if(this.tapDataIsSet){
            tapData.serialise(prefix + "" + 1 + "." , sb);
        }

        return sb;
    }

    private void deserialise(GolgiPayload payload){
        if(!isCorrupt() && payload.containsNestedKey("1")){
            TapData inst = new TapData(payload.getNested("1"));
            setTapData(inst);
               if(inst == null || inst.isCorrupt()){
                   setCorrupt();
               }
        }
        else {
            setCorrupt();
        }
    }

    public TapTelegraph_sendTap_reqArg(){
        this(true);
    }

    public TapTelegraph_sendTap_reqArg(boolean isSetDefault){
        super();
        tapDataIsSet = isSetDefault;
        tapData = new TapData(isSetDefault);
    }

    public TapTelegraph_sendTap_reqArg(GolgiPayload payload){
        this(false);
        deserialise(payload);
    }

    public TapTelegraph_sendTap_reqArg(String payload){
        this(JavaType.createPayload(payload));
    }

}
